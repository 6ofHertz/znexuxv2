# 🔄 ZURVAN Data Flow Visualization

## Complete Data Journey: User → UI → Firestore → UI

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER AUTHENTICATION                          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                    ┌──────────────────┐
                    │  Firebase Auth   │
                    │  Creates Session │
                    │  Returns user    │
                    └──────────────────┘
                              ↓
                    ┌──────────────────┐
                    │  AuthContext     │
                    │  Stores user     │
                    │  user.uid = "abc"│
                    └──────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      DATA LOADING (READ)                         │
└─────────────────────────────────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │        Index.tsx (Main Page)          │
          │                                       │
          │  useEffect(() => {                    │
          │    getTasks(user.uid)                 │
          │    getStreams(user.uid)               │
          │  }, [user])                           │
          └───────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │   src/lib/firebase/firestore.ts       │
          │                                       │
          │  getTasks(userId) {                   │
          │    query(tasks,                       │
          │      where('user_id', '==', userId))  │
          │  }                                    │
          └───────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │         FIRESTORE DATABASE            │
          │                                       │
          │  Collection: tasks                    │
          │  ├─ task_1 (user_id: "abc")           │
          │  ├─ task_2 (user_id: "abc")           │
          │  └─ task_3 (user_id: "xyz") ← filtered│
          │                                       │
          │  Security Rules Check:                │
          │  ✅ user_id === auth.uid              │
          │  ❌ Returns only "abc" tasks          │
          └───────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │          RETURNS DATA                 │
          │                                       │
          │  [                                    │
          │    { id: "task_1", user_id: "abc",    │
          │      title: "Learn React",            │
          │      completed: false },              │
          │    { id: "task_2", user_id: "abc",    │
          │      title: "Build app",              │
          │      completed: true }                │
          │  ]                                    │
          └───────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │       UPDATE LOCAL STATE              │
          │                                       │
          │  setTasks([...tasks])                 │
          │  setStreams([...streams])             │
          └───────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │         UI RENDERS                    │
          │                                       │
          │  <TodaysFocus tasks={tasks} />        │
          │  <StreamCard streams={streams} />     │
          └───────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                    DATA CREATION (WRITE)                         │
└─────────────────────────────────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │       USER CREATES TASK               │
          │                                       │
          │  [Create Task Button Clicked]         │
          │  Form data: {                         │
          │    title: "New Task",                 │
          │    completed: false,                  │
          │    estimatedMinutes: 30               │
          │  }                                    │
          └───────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │    Component Handler Called           │
          │                                       │
          │  const handleCreate = async () => {   │
          │    const newTask = await createTask(  │
          │      user.uid,                        │
          │      formData                         │
          │    );                                 │
          │  }                                    │
          └───────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │   src/lib/firebase/firestore.ts       │
          │                                       │
          │  createTask(userId, data) {           │
          │    addDoc(collection(db, 'tasks'), {  │
          │      ...data,                         │
          │      user_id: userId,                 │
          │      created_at: serverTimestamp()    │
          │    })                                 │
          │  }                                    │
          └───────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │      FIRESTORE SECURITY CHECK         │
          │                                       │
          │  Rule: allow create if               │
          │    isAuthenticated() &&               │
          │    request.resource.data.user_id      │
          │      == request.auth.uid              │
          │                                       │
          │  Validation:                          │
          │  ✅ User authenticated                │
          │  ✅ user_id matches auth.uid          │
          │  ✅ ALLOW OPERATION                   │
          └───────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │      DOCUMENT CREATED                 │
          │                                       │
          │  Collection: tasks                    │
          │  Document ID: auto_generated_123      │
          │  {                                    │
          │    id: "auto_generated_123",          │
          │    user_id: "abc",                    │
          │    title: "New Task",                 │
          │    completed: false,                  │
          │    estimatedMinutes: 30,              │
          │    created_at: 2025-01-15T10:30:00Z   │
          │  }                                    │
          └───────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │        RETURNS NEW TASK               │
          │                                       │
          │  { id: "auto_generated_123", ... }    │
          └───────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │      UPDATE LOCAL STATE               │
          │                                       │
          │  setTasks([newTask, ...tasks])        │
          └───────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │           UI UPDATES                  │
          │                                       │
          │  New task appears in list!            │
          │  toast.success('Task created!')       │
          └───────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                    DATA UPDATE (MODIFY)                          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │   USER TOGGLES TASK COMPLETION        │
          │                                       │
          │  [Checkbox Clicked]                   │
          │  Task ID: "task_123"                  │
          │  Current: completed = false           │
          │  Desired: completed = true            │
          └───────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │     Handler in Index.tsx              │
          │                                       │
          │  handleToggleTask(taskId) {           │
          │    await updateTask(taskId, {         │
          │      completed: !task.completed       │
          │    });                                │
          │  }                                    │
          └───────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │   src/lib/firebase/firestore.ts       │
          │                                       │
          │  updateTask(taskId, updates) {        │
          │    updateDoc(doc(db,'tasks',taskId),{ │
          │      ...updates,                      │
          │      updated_at: serverTimestamp()    │
          │    })                                 │
          │  }                                    │
          └───────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │      FIRESTORE SECURITY CHECK         │
          │                                       │
          │  Rule: allow update if               │
          │    isAuthenticated() &&               │
          │    resource.data.user_id              │
          │      == request.auth.uid              │
          │                                       │
          │  Checks existing document:            │
          │  ✅ Document exists                   │
          │  ✅ user_id === auth.uid              │
          │  ✅ ALLOW OPERATION                   │
          └───────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │       DOCUMENT UPDATED                │
          │                                       │
          │  tasks/task_123                       │
          │  {                                    │
          │    ...existing_fields,                │
          │    completed: true, ← CHANGED         │
          │    updated_at: 2025-01-15T10:35:00Z   │
          │  }                                    │
          └───────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │      UPDATE LOCAL STATE               │
          │                                       │
          │  setTasks(tasks.map(t =>              │
          │    t.id === taskId                    │
          │      ? {...t, completed: true}        │
          │      : t                              │
          │  ))                                   │
          └───────────────────────────────────────┘
                              ↓
          ┌───────────────────────────────────────┐
          │           UI UPDATES                  │
          │                                       │
          │  Checkbox now checked ✓               │
          │  toast.success('Task completed!')     │
          │                                       │
          │  Audit log created:                   │
          │  { action: "task_update", ... }       │
          └───────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                   SECURITY ENFORCEMENT                           │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  Scenario 1: User "abc" tries to access own data                │
└─────────────────────────────────────────────────────────────────┘
                              ↓
          Request: getTasks("abc")
          Auth Token: user_id = "abc"
                              ↓
          Firestore Query: where('user_id', '==', 'abc')
                              ↓
          Security Rule: resource.data.user_id == request.auth.uid
          Check: "abc" === "abc" ✅
                              ↓
          RESULT: Returns all tasks for user "abc"


┌─────────────────────────────────────────────────────────────────┐
│  Scenario 2: User "abc" tries to access "xyz" data              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
          Request: getTasks("xyz")
          Auth Token: user_id = "abc"
                              ↓
          Firestore Query: where('user_id', '==', 'xyz')
          Security Rule: resource.data.user_id == request.auth.uid
          Check: "xyz" === "abc" ❌
                              ↓
          RESULT: Returns empty array (no error, just no data)


┌─────────────────────────────────────────────────────────────────┐
│  Scenario 3: Unauthenticated user tries to access data          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
          Request: getTasks("abc")
          Auth Token: null (not logged in)
                              ↓
          Security Rule: isAuthenticated() returns false
                              ↓
          RESULT: Permission denied error
                  User redirected to /auth page


┌─────────────────────────────────────────────────────────────────┐
│                      KEY TAKEAWAYS                               │
└─────────────────────────────────────────────────────────────────┘

1. Authentication Required:
   ✅ User must sign in via Firebase Auth first
   ✅ Every request includes auth token (automatic)

2. Data Isolation:
   ✅ Users only see their own data
   ✅ Enforced by Firestore security rules
   ✅ No way to bypass (server-side validation)

3. Automatic Protection:
   ✅ user_id always validated against auth.uid
   ✅ Timestamps auto-generated by server
   ✅ No client-side manipulation possible

4. Real-time Sync:
   ✅ Changes in Firestore reflect immediately
   ✅ Local state updated after operations
   ✅ UI re-renders automatically

5. Type Safety:
   ✅ Full TypeScript support
   ✅ Compile-time error catching
   ✅ IntelliSense for all fields
```

## 🎯 Quick Reference

**Create Data:**
```typescript
const task = await createTask(user.uid, { title: "New Task" });
```

**Read Data:**
```typescript
const tasks = await getTasks(user.uid);
```

**Update Data:**
```typescript
await updateTask(taskId, { completed: true });
```

**Delete Data:**
```typescript
await deleteTask(taskId);
```

**All operations automatically:**
- ✅ Validate authentication
- ✅ Check user ownership
- ✅ Handle timestamps
- ✅ Return typed data
